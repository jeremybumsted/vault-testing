steps:
  - label: ":go: :test_tube: Run tests"
    command: "./gotest.sh"
    # plugins:
    #   - buildkite-plugins/vault-secrets#update-auth:
    #       server: localhost
    #       path: buildkite/agent
    #       role: buildkite-agent
    #       secrets:
    #         - test-analytics
